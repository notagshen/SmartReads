version: '3.8'

services:
  smartreads-web:
    build: .
    ports:
      - "9220:4173"
    environment:
      - NODE_ENV=production
      - UPSTREAM_API_BASE_URL=https://axonhub.052222.xyz/v1
    restart: unless-stopped